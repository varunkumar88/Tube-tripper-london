// An enum with the name of the environments available
// so that different configurations can be used in different environments
module.exports = envEnum = {
  PRODUCTION: 'production',
  DEVELOPMENT: 'development',
};